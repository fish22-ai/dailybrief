# 密钥绝不进版本库。key 走 GitHub Secrets（云端）或环境变量（本机）
.env

# 每次运行都会重建，不需要版本化
cache/
logs/

# 本地调试用的临时脚本：文件留在工作区，但不进版本库。
# 光靠 git rm --cached 不够 —— 它会变成「未跟踪文件」，正好撞上 daily.bat 的预检。
_measure.js

__pycache__/
*.pyc
.venv/
